{
  "name": "semgrep-lsp-server",
  "description": "Language Server for Semgrep",
  "version": "0.1.0",
  "engines": {
    "vscode": "^1.66.0"
  },
  "dependencies": {
    "vscode-languageserver": "^7.0.0",
    "vscode-languageserver-textdocument": "^1.0.4"
  },
  "scripts": {
    "clean": "rm -rf ./out",
    "lint": "eslint src --ext ts"
  }
}
